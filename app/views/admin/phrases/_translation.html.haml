%fieldset.translation
  = t.input :language, collection: @supported_languages.map {|l| [l.name, l.language]}, prompt: 'Sprache auswählen...'
  = t.input :text
  = t.input :transliteration
  = render partial: "application/record_with_#{browser.audio_recording_capabilities.first}", locals: {f: t}

%h1 Listing translations

%table
  %thead
    %tr
      %th Phrase
      %th Original
      %th Transliteration
      %th Language
      %th Source Country
      %th
      %th

  %tbody
    - @translations.each do |translation|
      %tr
        %td= translation.phrase.text if translation.phrase
        %td= translation.original
        %td= translation.transliteration
        %td= translation.language
        %td= translation.source_country
        %td= link_to 'Edit', edit_translation_path(translation)
        %td= link_to 'Destroy', translation, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Translation', new_translation_path

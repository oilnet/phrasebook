= form_tag(phrases_path, method: :get, id: :search, class: 'search-bar', role: :search, remote: true) do
  = text_field_tag :search, params[:search], placeholder: 'Thema oder Wort?', type: :search, autofocus: true, autocomplete: :off
  = button_tag type: :submit do
    = image_tag 'search-icon-black.png', alt: 'Suchen'
#phrases_list
  = render @phrases
